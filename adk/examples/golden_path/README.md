# Golden Path Example

This example demonstrates the standard lifecycle of an artifact under the ADK contracts.

## Running
```bash
../../cli/adk.sh run .
```

## Expected Output
A set of signed event logs and artifact records in the `output/` directory, matching the schemas in `contracts/v0`.
